name: Usage notifications
question: Do you notify users if usage nears service limits?
filter_label: notifies users if usage nears service limits

depends:
  - "on": lot
    being:
      - cloud-hosting
followup:
  usageNotificationsHow:
    - true

type: boolean

validations:
  - name: answer_required
    message: You need to answer this question.
