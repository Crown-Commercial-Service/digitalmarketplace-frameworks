name: Data export formats
question: What other formats can users export their data in?

optional: true
depends:
  - "on": lot
    being:
      - cloud-software

type: list
list_item_name: data format

validations:
  - name: under_10_words
    message: Each data format must be no more than 10 words.
  - name: max_items_limit
    message: You must have 10 or fewer data formats.
  - name: under_character_limit
    message: Each data format must be no more than 100 characters.
