name: Scaling notifications
question: Do you notify users about if usage nears service limits?

depends:
  - "on": lot
    being:
      - cloud-hosting

type: boolean

validations:
  - name: answer_required
    message: You need to answer this question.
