name: User authentication needed
question: Do users need to be authenticated when using your service?

depends:
  - "on": lot
    being:
      - cloud-software

type: boolean

validations:
  - name: answer_required
    message: You need to answer this question.
