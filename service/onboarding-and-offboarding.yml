Onboarding and offboarding:
  Service onboarding process included:
    dependsOnLots: SaaS, PaaS, IaaS
    fields:
      - type: boolean
    validationNotAnswered: "Please answer if a service onboarding process is included"
  Service offboarding process included:
    dependsOnLots: SaaS, PaaS, IaaS
    fields:
      - type: boolean
    validationNotAnswered: "Please answer if a service offboarding process is included"
