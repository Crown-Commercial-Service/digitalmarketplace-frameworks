Cloud features:
  Elastic cloud approach supported:
    dependsOnLots: SaaS, PaaS, IaaS
    fields:
      - type: boolean
    validationNotAnswered: "Please answer if elastic cloud approach is supported"
  Guaranteed resources defined:
    dependsOnLots: SaaS, PaaS, IaaS
    fields:
      - type: boolean
    validationNotAnswered: "Please answer if guaranteed resources are defined"
  Persistent storage supported:
    dependsOnLots: SaaS, PaaS, IaaS
    fields:
      - type: boolean
    validationNotAnswered: "Please answer if persistent storage is supported"
